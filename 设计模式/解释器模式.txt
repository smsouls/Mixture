解释器模式

角色：
1.AbstractException:抽象解释器
2.TerminalException:终结符表达式
3.NonterminalException:非终结符表达式

使用场景：
重复发生的问题可以使用解释器。如多个服务器，每天产生大量的日志，需要对这些日志文件进行分析处理，
由于每个服务器的日志格式不同，但是数据要素是相同的，也就是终结表达式一致，非终结表达式不一致。

